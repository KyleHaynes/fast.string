{
  "hash": "98fc07830958349fbb7030a98246a2b0",
  "result": {
    "engine": "knitr",
    "markdown": "# Preface {.unnumbered}\n\n`fast.string` is an R package of drop-in-faster equivalents for the string,\ndate, and phonetic-matching functions that show up constantly in record\nlinkage and ETL work — `grepl()`, `grep()`, `sub()`, `gsub()`, `trimws()`,\n`substr()`, `nchar()`, `chartr()`, plus a handful of functions base R simply\ndoesn't have at all: Soundex/NYSIIS phonetic codes, and vectorised\nJaro-Winkler similarity (including a token-reorder/punctuation-aware\nvariant for multi-word names).\n\nEverything is parallelised across every CPU core via Intel TBB (through\n[RcppParallel](https://rcppcore.github.io/RcppParallel/)), with\n[PCRE2](https://www.pcre.org/) doing the regex work and\n[RE2](https://github.com/google/re2)'s `StringPiece` handling fixed/literal\nmatching. Typical speedups over base R are **5-40x** on large character\nvectors; some operations (parallel Jaro-Winkler, multi-pattern substitution\nin one pass) have no base R equivalent at all.\n\n## Why this exists\n\nRecord linkage and data-cleaning pipelines spend a surprising fraction of\ntheir wall-clock time in string operations that look trivial in isolation —\n`trimws()` on a column, a `gsub()` to strip punctuation, `grepl()` to flag a\npattern — but get called over and over on multi-million-row name/address\nfiles. Individually each call is fast; in aggregate, on a 5M-row file, they\nadd up to minutes of CPU time spent doing work a single core could be doing\nacross eight or sixteen instead.\n\nThree things are conspicuously *not* one function each in base R, despite\nbeing everywhere in this kind of pipeline:\n\n- **Phonetic blocking keys** (Soundex, NYSIIS) — used to group\n  *probably-same* names before running an expensive pairwise comparison\n  within each group, rather than comparing every record against every\n  other record.\n- **Fuzzy string similarity** (Jaro-Winkler) — base R has no notion of\n  \"how similar are these two strings,\" only exact (`==`) or pattern\n  (`grepl()`) matching.\n- **Multi-token name comparison** — `\"Kyle John Haynes\"` and\n  `\"John Kylie Haynes\"` are obviously the same person with two tokens\n  swapped, but a plain character-by-character comparison scores that badly.\n\n`fast.string` exists to make all of the above fast enough to run\nunblinkingly on real-sized data, and to package the boring-but-essential\n80% (trimming, substitution, date parsing) so a project doesn't need five\ndifferent dependencies for five different speed problems.\n\n## What this is *not*\n\nTo set expectations: `fast.string` is a toolbox of vectorised primitives,\nnot a record-linkage framework. It doesn't do blocking-strategy selection,\nEM-based match probability estimation, or persistence of a linked dataset —\nfor that end of the problem, pair it with something like\n[`RecordLinkage`](https://cran.r-project.org/package=RecordLinkage) or\n[`reclin2`](https://cran.r-project.org/package=reclin2), and use\n`fast.string` for the per-string comparison work those packages call\nunderneath. @sec-benchmarks compares `fast.string`'s comparison primitives\ndirectly against the equivalent functions in `RecordLinkage` and\n`stringdist` at scale.\n\n## How this book is organised\n\n- **Getting started** — what `fast.string` is for and how it's positioned\n  against base R, `stringi`/`stringr`, `stringdist`, and `RecordLinkage`\n  (@sec-about), then a hands-on tour of every function group\n  (@sec-quickstart).\n- **Under the hood** — the algorithms (@sec-theory): why two regex\n  engines, how the bit-parallel Jaro-Winkler implementation works, how\n  multi-token name comparison resolves reordered tokens — followed by a\n  full worked record-linkage pipeline (@sec-workflow) putting the pieces\n  together.\n- **Reference** — an exhaustive per-function reference with every\n  argument and runnable examples (@sec-reference); the heavy benchmark\n  chapter at **3 million strings**, with `base`, `stringi`, `stringdist`,\n  and `RecordLinkage` as comparison baselines (@sec-benchmarks); and a\n  troubleshooting/FAQ chapter (@sec-troubleshooting).\n\n## A quick taste\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn <- 2e5\nx <- sprintf(\"%s-%04d\", sample(c(\"John Smith\", \"Mary O'Brien\", \"Kyle Haynes\"), n, TRUE), sample(9999, n, TRUE))\n\nsystem.time(base::grepl(\"Smith\", x))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n   user  system elapsed \n   0.03    0.00    0.03 \n```\n\n\n:::\n\n```{.r .cell-code}\nsystem.time(fast.string::fgrepl(\"Smith\", x))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n   user  system elapsed \n      0       0       0 \n```\n\n\n:::\n\n```{.r .cell-code}\nround(fast.string::jaro_winkler_tokens(\"Kyle John Haynes\", \"John Kylie Haynes\"), 3)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 0.984\n```\n\n\n:::\n:::\n\n\nThat last call is the headline example for the multi-token comparison\nfunction: two names with reordered tokens and one typo still score above\n0.95. The full story — including why it also handles `\"O'Brien\"` vs.\n`\"O Brien\"` vs. `\"OBrien\"` — is in @sec-theory and @sec-reference.\n",
    "supporting": [],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}