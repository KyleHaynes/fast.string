% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nysiis.R
\name{nysiis}
\alias{nysiis}
\title{NYSIIS phonetic code (New York State Identification and Intelligence System)}
\usage{
nysiis(x)
}
\arguments{
\item{x}{Character vector (coerced via \code{\link[=as.character]{as.character()}} if not already
character). \code{NA} elements, and elements with no alphabetic characters,
return \code{NA}.}
}
\value{
Character vector the same length as \code{x}, with \code{names(x)}
preserved, each element either \code{NA} or up to 6 characters.
}
\description{
Encodes each string as a NYSIIS phonetic key (commonly-cited core
ruleset: leading/trailing letter transforms, a per-letter transform
table with adjacent-duplicate collapsing, and trailing key cleanup),
truncated to 6 characters. Has no \link{base} equivalent; intended to
complement \code{\link[=jaro_winkler]{jaro_winkler()}} / \code{\link[=jaro_winkler_matrix]{jaro_winkler_matrix()}} as a blocking key
for fuzzy name matching, typically alongside or instead of \code{\link[=soundex]{soundex()}}.
}
