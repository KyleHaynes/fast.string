% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/double_metaphone.R
\name{double_metaphone}
\alias{double_metaphone}
\title{Double Metaphone phonetic code (Lawrence Philips, 2000)}
\usage{
double_metaphone(x)
}
\arguments{
\item{x}{Character vector (coerced via \code{\link[=as.character]{as.character()}} if not already
character). \code{NA} elements, and elements with no content after
trimming, return \code{NA} in both columns.}
}
\value{
A \code{data.frame} with two character columns, \code{primary} and
\code{secondary}, each \code{length(x)} long (\code{secondary} is empty \code{""}, not
\code{NA}, when the algorithm found no plausible alternate pronunciation —
only \code{NA} input produces \code{NA} output).
}
\description{
An improvement on classic Metaphone that copes with names of non-English
origin by returning two codes per word — \code{primary} and \code{secondary} — when
the pronunciation is ambiguous (e.g. a \code{C} that could plausibly be a hard
\code{K} or a soft \code{S} sound). Two names are usually considered a phonetic
match if \emph{any} of \code{primary}/\code{secondary} from one matches \emph{any} of
\code{primary}/\code{secondary} from the other. More discriminating than
\code{\link[=soundex]{soundex()}} or \code{\link[=nysiis]{nysiis()}} for names outside their English/American
design target, at the cost of a considerably more involved ruleset.
}
\details{
Ported from the structure of the widely-used Apache Commons Codec Java
implementation and cross-checked against its published test vectors (see
\code{tests/testthat/test-double_metaphone.R}). Unlike \code{\link[=soundex]{soundex()}}/\code{\link[=nysiis]{nysiis()}},
which strip the input down to letters only before encoding, this keeps
internal whitespace and punctuation (only trimming the ends and
uppercasing) — the algorithm itself keys off literal fragments like
\code{"VAN "}/\code{"SAN "}/\code{"SCH"}, including the space.
}
\examples{
double_metaphone(c("Smith", "Schmidt", "Catherine", "Kathryn"))
}
\seealso{
\code{\link[=soundex]{soundex()}}, \code{\link[=nysiis]{nysiis()}}, \code{\link[=caverphone]{caverphone()}}
}
