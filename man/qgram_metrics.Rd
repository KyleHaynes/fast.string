% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qgram_metrics.R
\name{qgram_metrics}
\alias{qgram_metrics}
\alias{jaccard_index}
\alias{dice_coefficient}
\alias{tversky_index}
\title{Vectorised q-gram set-overlap similarity: Jaccard, Dice, Tversky}
\usage{
jaccard_index(a, b, q = 2, nthreads = NULL)

dice_coefficient(a, b, q = 2, nthreads = NULL)

tversky_index(a, b, q = 2, alpha = 0.5, beta = 0.5, nthreads = NULL)
}
\arguments{
\item{a, b}{Equal-length character vectors.}

\item{q}{Q-gram length (default \code{2}, i.e. bigrams — the common default).
Must be a single integer >= 1.}

\item{nthreads}{Positive integer per-call thread cap, or \code{NULL} to use the
RcppParallel default. \code{1} forces serial execution.}

\item{alpha, beta}{Tversky asymmetry weights (default \code{0.5} each, matching
Dice). Non-negative scalars.}
}
\value{
Numeric vector of similarities in \verb{[0, 1]}, \code{length(a)} long.
\code{NA} if either \code{a[i]} or \code{b[i]} is \code{NA}. Two strings shorter than \code{q}
(so neither has any q-grams) compare equal (\code{1}).
}
\description{
Three related measures of how much two strings' sets of overlapping
length-\code{q} substrings ("q-grams") have in common. Split each string into
its q-gram set, then:
}
\details{
\itemize{
\item \code{jaccard_index()} — \verb{|intersection| / |union|}.
\item \code{dice_coefficient()} — \verb{2*|intersection| / (|A| + |B|)}, the
Sorensen-Dice coefficient; weights the shared q-grams more heavily than
Jaccard does (it's always >= the Jaccard score for the same pair).
\item \code{tversky_index()} — generalises both: `|intersection| / (|intersection|
\itemize{
\item alpha*|A only| + beta*|B only|)\code{. }alpha = beta = 1\verb{reduces to Jaccard;}alpha = beta = 0.5\verb{ reduces to Dice. Asymmetric weights (}alpha != beta`) are useful when one side is treated as a query and
the other as a reference and the two kinds of mismatch shouldn't be
penalised equally — e.g. extra tokens on the reference side mattering
less than missing tokens from the query.
}
}

All three are symmetric in token order and insensitive to \emph{where} a
difference occurs, which makes them a useful second signal alongside
edit-distance-style metrics like \code{\link[=jaro_winkler]{jaro_winkler()}} for fuzzy matching.

The q-gram set is built and deduplicated as packed integers rather than
allocated strings (for \code{q <= 8}, which covers the overwhelming majority of
use), and the three measures share one sorted-merge intersection pass —
faster than \code{stringdist::stringdist(method = "jaccard")} for the same
reason \code{\link[=jaro_winkler]{jaro_winkler()}} is faster than \code{stringdist}'s Jaro-Winkler: no
per-pair R-level dispatch, and the whole vector runs in parallel across
CPU cores via Intel TBB (through RcppParallel).
}
\examples{
jaccard_index("night", "nacht")
dice_coefficient("night", "nacht")
tversky_index("night", "nacht", alpha = 1, beta = 1) # == jaccard_index()
jaccard_index(c("Kyle Haynes", "John Smith"), c("Kyle Haynes", "Jon Smith"))
}
\seealso{
\code{\link[=jaccard_matrix]{jaccard_matrix()}}, \code{\link[=dice_matrix]{dice_matrix()}}, \code{\link[=tversky_matrix]{tversky_matrix()}}, \code{\link[=jaro_winkler]{jaro_winkler()}}
}
