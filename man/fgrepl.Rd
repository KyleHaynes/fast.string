% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fast_grepl.R
\name{fgrepl}
\alias{fgrepl}
\title{Fast parallel string matching (masks base::grepl)}
\usage{
fgrepl(
  pattern,
  x,
  ignore.case = FALSE,
  perl = FALSE,
  fixed = FALSE,
  useBytes = FALSE,
  verbose = getOption("fast.string.verbose", TRUE),
  nthreads = NULL
)
}
\arguments{
\item{pattern}{Character scalar. Pattern to search for.}

\item{x}{Character vector. \code{NA} elements return \code{NA}.}

\item{ignore.case}{Logical. Case-insensitive matching.}

\item{perl}{Logical. If \code{TRUE}, skip the PCRE-only syntax check and run
the parallel PCRE2 engine directly.}

\item{fixed}{Logical. Treat \code{pattern} as a literal string (fastest path).}

\item{useBytes}{Logical. Ignored; included for signature compatibility.}

\item{verbose}{Logical. Show a one-time message that base functions are
masked. Defaults to \code{getOption("fast.string.verbose", TRUE)}.}

\item{nthreads}{Integer or \code{NULL}. Thread count; \code{NULL} uses all cores.}
}
\value{
Logical vector the same length as \code{x}.
}
\description{
Drop-in replacement for \code{\link[base:grep]{base::grepl()}} using PCRE2 and Intel TBB.
Typically 5–20x faster on large character vectors.
}
\details{
Patterns using PCRE-only syntax (lookaheads, lookbehinds, atomic groups,
possessive quantifiers, named backreferences) are automatically detected
when \code{perl = FALSE} and delegated to \code{\link[base:grep]{base::grepl()}} with \code{perl = TRUE},
so results are always correct.
}
\seealso{
\code{\link[base:grep]{base::grepl()}}
}
