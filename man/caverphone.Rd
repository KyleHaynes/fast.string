% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/caverphone.R
\name{caverphone}
\alias{caverphone}
\title{Caverphone 2.0 phonetic code (Caversham Project, University of Otago)}
\usage{
caverphone(x)
}
\arguments{
\item{x}{Character vector (coerced via \code{\link[=as.character]{as.character()}} if not already
character). \code{NA} elements return \code{NA}; every other input — including
\code{""} — returns a 10-character code (\code{""} and pure-non-letter strings
both code as \code{"1111111111"}).}
}
\value{
Character vector the same length as \code{x}, with \code{names(x)}
preserved, each element either \code{NA} or exactly 10 characters.
}
\description{
Encodes each string as a fixed 10-character phonetic code via a chain of
substring transforms originally designed for matching New Zealand
electoral-roll names, and widely used in NZ/AU record linkage generally.
Complements \code{\link[=soundex]{soundex()}}/\code{\link[=nysiis]{nysiis()}}/\code{\link[=double_metaphone]{double_metaphone()}} as another
blocking-key option — different rulesets group different near-miss
spellings together, so trying more than one can catch matches a single
phonetic key misses.
}
\details{
Ported step-for-step from the algorithm's reference Java implementation
(Apache Commons Codec's \code{Caverphone2}) and cross-checked against its
published test vectors (see \code{tests/testthat/test-caverphone.R}),
including the "enough"/"trough" duplicate-rule quirk inherited from the
original published 2004 specification — preserved here rather than
"corrected", since the point of a phonetic key is to match the standard
algorithm everyone else is using, not a personal improvement on it.
}
\examples{
caverphone(c("Peter", "Tedder", "Stevenson"))
}
\seealso{
\code{\link[=soundex]{soundex()}}, \code{\link[=nysiis]{nysiis()}}, \code{\link[=double_metaphone]{double_metaphone()}}
}
