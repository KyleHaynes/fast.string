% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsub_all.R
\name{gsub_all}
\alias{gsub_all}
\title{Apply multiple substitutions in one pass}
\usage{
gsub_all(
  patterns,
  replacements,
  x,
  fixed = FALSE,
  ignore.case = FALSE,
  sequential = TRUE,
  nthreads = NULL
)
}
\arguments{
\item{patterns}{Character vector of patterns to search for.}

\item{replacements}{Character scalar or vector the same length as
\code{patterns}.}

\item{x}{Character vector. \code{NA} elements return \code{NA}.}

\item{fixed}{Logical. Treat each pattern as a literal string.}

\item{ignore.case}{Logical. Case-insensitive matching.}

\item{sequential}{Logical. If \code{TRUE} (default), apply patterns one after
another (later patterns can match text introduced by earlier
replacements). If \code{FALSE}, match all patterns in a single left-to-right
scan (first pattern to match at each position wins). This mode requires
\code{fixed = TRUE}.}

\item{nthreads}{Positive integer per-call thread cap, or \code{NULL} to use the
RcppParallel default. \code{1} forces serial execution.}
}
\value{
Character vector the same length as \code{x}.
}
\description{
Replace several patterns in \code{x} either sequentially (one full pass per
pattern, like chaining \code{\link[=fgsub]{fgsub()}} calls) or, for fixed patterns, in a single
combined scan (\code{sequential = FALSE}), parallelised across CPU cores via
Intel TBB. Non-sequential regular-expression replacement is rejected
because combined capture/backreference semantics are not implemented.
}
\examples{
x <- c("cat and hat", "a cat")
gsub_all(c("cat", "hat"), c("dog", "cap"), x, fixed = TRUE)

# Sequential mode chains the patterns, so "a" -> "b" -> "c" cascades...
gsub_all(c("a", "b"), c("b", "c"), "ab", fixed = TRUE)
# ...whereas a single scan replaces each position once.
gsub_all(c("a", "b"), c("b", "c"), "ab", fixed = TRUE, sequential = FALSE)
}
\seealso{
\code{\link[=fgsub]{fgsub()}} for a single pattern.

Other matching and substitution functions:
\code{\link[=fcount]{fcount()}},
\code{\link[=fgrep]{fgrep()}},
\code{\link[=fgrepl]{fgrepl()}},
\code{\link[=fgsub]{fgsub()}},
\code{\link[=fsub]{fsub()}}
}
\concept{matching and substitution functions}
