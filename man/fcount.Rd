% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fast_grepl.R
\name{fcount}
\alias{fcount}
\title{Fast parallel match counting}
\usage{
fcount(
  pattern,
  x,
  ignore.case = FALSE,
  perl = FALSE,
  fixed = FALSE,
  useBytes = FALSE,
  nthreads = NULL
)
}
\arguments{
\item{pattern}{Character scalar. Pattern to search for.}

\item{x}{Character vector. \code{NA} elements return \code{NA}.}

\item{ignore.case}{Logical. Case-insensitive matching.}

\item{perl}{Logical. If \code{TRUE}, skip the PCRE-only syntax check and run
the parallel PCRE2 engine directly.}

\item{fixed}{Logical. Treat \code{pattern} as a literal string (fastest path).}

\item{useBytes}{Logical. Ignored; included for signature compatibility.}

\item{nthreads}{Positive integer per-call thread cap, or \code{NULL} to use the
RcppParallel default. \code{1} forces serial execution.}
}
\value{
Integer vector the same length as \code{x}. Missing inputs return \code{NA}.
}
\description{
Counts non-overlapping matches of one pattern in each element of \code{x}.
This is the counting counterpart to \code{\link[=fgrepl]{fgrepl()}}, using the same PCRE2 and
prepared fixed-string engines and the same parallel dispatch policy.
}
\examples{
x <- c("apple pie", "banana split", NA, "cherry tart")

fcount("a", x, fixed = TRUE)
fcount("[aeiou]", x)            # vowels per string
}
\seealso{
\code{\link[base:gregexpr]{base::gregexpr()}}

Other matching and substitution functions:
\code{\link[=fgrep]{fgrep()}},
\code{\link[=fgrepl]{fgrepl()}},
\code{\link[=fgsub]{fgsub()}},
\code{\link[=fsub]{fsub()}},
\code{\link[=gsub_all]{gsub_all()}}
}
\concept{matching and substitution functions}
