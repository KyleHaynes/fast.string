% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fast.string-package.R
\docType{package}
\name{fast.string-package}
\alias{fast.string}
\alias{fast.string-package}
\title{fast.string: parallel string, date and fuzzy-matching functions}
\description{
Fast, multi-core alternatives to base R's string functions, plus the
building blocks of a record-linkage pipeline: fixed-format date and
timestamp parsing, phonetic blocking keys, string-similarity and
edit-distance measures, and memory-bounded fuzzy lookup.
}
\details{
Regular expressions run on PCRE2 and fixed strings on a prepared literal
searcher, with work split across cores by Intel TBB (through
RcppParallel). Small inputs are processed serially in the calling thread;
larger ones are divided between threads.

Loading the package prints the function index below as a tree. Silence it
with \code{options(fast.string.verbose = FALSE)} before \code{\link[=library]{library()}}, or wrap the
call in \code{\link[=suppressPackageStartupMessages]{suppressPackageStartupMessages()}}.

In interactive sessions, calling a base function that has a fast.string
equivalent (\code{grepl()}, \code{grep()}, \code{sub()}, \code{gsub()}, \code{trimws()}, \code{substr()},
\code{nchar()}, \code{chartr()}) prints a one-time-per-function reminder naming the
faster drop-in; the base function still runs unchanged either way. Turn
this off with \code{options(fast.string.reminders = FALSE)}.
}
\section{Function index}{

\itemize{
\item \strong{Matching & substitution}
\itemize{
\item \code{\link[=fgrepl]{fgrepl()}} - grepl() equivalent
\item \code{\link[=fgrep]{fgrep()}} - grep() equivalent
\item \code{\link[=fcount]{fcount()}} - count matches per string
\item \code{\link[=fsub]{fsub()}} - sub() equivalent
\item \code{\link[=fgsub]{fgsub()}} - gsub() equivalent
\item \code{\link[=gsub_all]{gsub_all()}} - many patterns in one pass
}
\item \strong{String utilities}
\itemize{
\item \code{\link[=ftrimws]{ftrimws()}} - trimws() equivalent
\item \code{\link[=fsubstr]{fsubstr()}} - substr() equivalent
\item \code{\link[=fnchar]{fnchar()}} - nchar() equivalent
\item \code{\link[=fchartr]{fchartr()}} - chartr() equivalent
}
\item \strong{Dates & timestamps}
\itemize{
\item \emph{Parse}
\itemize{
\item \code{\link[=fas.Date]{fas.Date()}} - parse fixed-format dates
\item \code{\link[=fas.POSIXct]{fas.POSIXct()}} - parse fixed-format timestamps
}
\item \emph{Format}
\itemize{
\item \code{\link[=format_date]{format_date()}} - format dates
\item \code{\link[=format_datetime]{format_datetime()}} - format timestamps
}
\item \emph{Parts}
\itemize{
\item \code{\link[=date_parts]{date_parts()}} - split a Date into y/m/d columns
\item \code{\link[=format_date_parts]{format_date_parts()}} - join y/m/d fields into a string
}
}
\item \strong{Phonetic codes}
\itemize{
\item \code{\link[=soundex]{soundex()}} - American Soundex
\item \code{\link[=refined_soundex]{refined_soundex()}} - Refined Soundex (finer blocks)
\item \code{\link[=nysiis]{nysiis()}} - NYSIIS
\item \code{\link[=cologne]{cologne()}} - Cologne (German pronunciation)
\item \code{\link[=double_metaphone]{double_metaphone()}} - Double Metaphone (two codes)
\item \code{\link[=caverphone]{caverphone()}} - Caverphone 2.0 (NZ/AU names)
}
\item \strong{String similarity}
\itemize{
\item \emph{Jaro-Winkler}
\itemize{
\item \code{\link[=jaro_winkler]{jaro_winkler()}} - pairwise similarity
\item \code{\link[=jaro_winkler_matrix]{jaro_winkler_matrix()}} - all-pairs similarity matrix
\item \code{\link[=jaro_winkler_tokens]{jaro_winkler_tokens()}} - word-order-tolerant similarity
}
\item \emph{Edit distance}
\itemize{
\item \code{\link[=levenshtein]{levenshtein()}} - insert/delete/substitute edits
\item \code{\link[=levenshtein_similarity]{levenshtein_similarity()}} - normalised 0-1 similarity
\item \code{\link[=levenshtein_matrix]{levenshtein_matrix()}} - all-pairs distance matrix
\item \code{\link[=levenshtein_within]{levenshtein_within()}} - TRUE if distance <= cutoff
\item \code{\link[=osa_distance]{osa_distance()}} - adds adjacent transpositions
\item \code{\link[=osa_similarity]{osa_similarity()}} - normalised 0-1 similarity
\item \code{\link[=osa_distance_matrix]{osa_distance_matrix()}} - all-pairs distance matrix
\item \code{\link[=damerau_levenshtein]{damerau_levenshtein()}} - unrestricted transpositions
\item \code{\link[=damerau_levenshtein_similarity]{damerau_levenshtein_similarity()}} - normalised 0-1 similarity
\item \code{\link[=damerau_levenshtein_matrix]{damerau_levenshtein_matrix()}} - all-pairs distance matrix
\item \code{\link[=hamming]{hamming()}} - count of differing positions
}
\item \emph{Q-gram overlap}
\itemize{
\item \code{\link[=jaccard_index]{jaccard_index()}} - Jaccard set overlap
\item \code{\link[=jaccard_matrix]{jaccard_matrix()}} - all-pairs matrix
\item \code{\link[=dice_coefficient]{dice_coefficient()}} - Dice set overlap
\item \code{\link[=dice_matrix]{dice_matrix()}} - all-pairs matrix
\item \code{\link[=tversky_index]{tversky_index()}} - asymmetric set overlap
\item \code{\link[=tversky_matrix]{tversky_matrix()}} - all-pairs matrix
\item \code{\link[=cosine_similarity]{cosine_similarity()}} - frequency-weighted overlap
\item \code{\link[=cosine_matrix]{cosine_matrix()}} - all-pairs matrix
}
\item \emph{fuzzywuzzy ratios}
\itemize{
\item \code{\link[=fuzz_ratio]{fuzz_ratio()}} - overall ratio (0-100)
\item \code{\link[=fuzz_partial_ratio]{fuzz_partial_ratio()}} - best-substring ratio
\item \code{\link[=fuzz_token_sort_ratio]{fuzz_token_sort_ratio()}} - ignores word order
\item \code{\link[=fuzz_token_set_ratio]{fuzz_token_set_ratio()}} - ignores extra/duplicate words
}
}
\item \strong{Fuzzy lookup}
\itemize{
\item \code{\link[=fuzzy_match]{fuzzy_match()}} - best table match per query
\item \code{\link[=fuzzy_top_n]{fuzzy_top_n()}} - top-N matches, no full matrix
}
\item \strong{Benchmarking}
\itemize{
\item \code{\link[=run_benchmark_example]{run_benchmark_example()}} - interactive speed demo vs base R
}
}
}

\section{Conventions}{

\itemize{
\item \strong{Missing values} propagate: an \code{NA} input gives an \code{NA} result, and the
comparison functions return \code{NA} when either side is \code{NA}.
\item \strong{Threads}: every parallel function takes \code{nthreads}. \code{NULL} (the
default) uses the RcppParallel setting, see
\code{\link[RcppParallel:setThreadOptions]{RcppParallel::setThreadOptions()}}; \code{1} forces serial execution.
\item \strong{Bytes or code points}: \code{\link[=jaro_winkler]{jaro_winkler()}} and the \link{edit_distance}
functions compare encoded bytes by default (\code{use_bytes = TRUE}) for
compatibility and speed; pass \code{use_bytes = FALSE} to compare UTF-8 code
points. \code{\link[=fuzzy_match]{fuzzy_match()}} and \code{\link[=fuzzy_top_n]{fuzzy_top_n()}} default to code points.
\item \strong{PCRE-only syntax}: \code{\link[=fgrepl]{fgrepl()}}, \code{\link[=fcount]{fcount()}}, \code{\link[=fsub]{fsub()}}, \code{\link[=fgsub]{fgsub()}} and
\code{\link[=gsub_all]{gsub_all()}} detect lookarounds, atomic groups and similar syntax when
\code{perl = FALSE}, and pass the call to base R with \code{perl = TRUE} (with a
message) so results stay correct.
}
}

\examples{
raw <- c("  Smith, John ", "SMITH,  JON", "Jones, Mary", NA)

# Clean: trim, collapse repeated whitespace, unify case.
clean <- toupper(fgsub("[[:space:]]+", " ", ftrimws(raw)))
clean

# Block on a phonetic key...
soundex(clean)

# ...then score candidates against a reference table.
reference <- c("JONES, MARY", "SMITH, JOHN")
round(jaro_winkler_matrix(clean, reference), 2)
fuzzy_match(clean, reference, min_score = 0.85)
}
\author{
\strong{Maintainer}: Kyle Haynes \email{kyle@kylehaynes.com.au}

Authors:
\itemize{
  \item Kyle Haynes \email{kyle@kylehaynes.com.au}
}

}
