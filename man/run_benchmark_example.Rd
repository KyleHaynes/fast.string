% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_benchmark_example.R
\name{run_benchmark_example}
\alias{run_benchmark_example}
\title{Interactive speed comparison: fast.string vs base R and other packages}
\usage{
run_benchmark_example(n = NULL, family = NULL, reps = NULL, seed = 1)
}
\arguments{
\item{n}{Number of test strings to generate. \code{NULL} (the default) prompts
for a value in interactive sessions, or uses \code{100000} otherwise.}

\item{family}{One of \code{"matching"}, \code{"strings"}, \code{"dates"}, \code{"similarity"},
\code{"phonetic"}, or \code{"all"}. \code{NULL} prompts in interactive sessions, or
uses \code{"all"} otherwise.}

\item{reps}{Number of timed repetitions per comparison; the fastest run
is reported. \code{NULL} prompts in interactive sessions, or uses \code{1}
otherwise.}

\item{seed}{Random seed for the generated test data, for reproducibility.}
}
\value{
Invisibly, a data frame with one row per (operation, baseline)
comparison: \code{operation}, \code{baseline}, \code{base_s}, \code{fast_s}, \code{speedup},
\code{speedup_label}, \code{match} (logical), \code{expect_match} (logical), and
\code{match_symbol}.
}
\description{
Runs a live, on-this-machine comparison of fast.string functions against
base R and — where installed — the alternative-package implementations
\code{docs/06-benchmarks.qmd} also compares against (\code{stringi} for matching and
string utilities; \code{stringdist} and \code{RecordLinkage} for string similarity
and edit distance; \code{RecordLinkage} and \code{phonics} for phonetic codes).
Comparisons whose package isn't installed are skipped, not errored.
}
\details{
Every comparison is a \strong{fresh random sample}, seeded by \code{seed}, and the
packages being compared can implement meaningfully different string or
regex conventions (PCRE2 here, base R's TRE/POSIX or \code{perl = TRUE}, ICU
in stringi, ...; different phonetic-algorithm revisions in \code{phonics}).
So a speed number alone doesn't establish the two sides computed the same
thing: after timing each comparison, this function also checks whether
fast.string's output and the baseline's output actually agree, and
reports that in the \code{Match} column and in a closing correctness summary.
A handful of comparisons are documented, known exceptions and are
labelled "differs (expected)" rather than flagged as a problem: several
phonetic comparisons implement a different algorithm/revision by design;
\code{base::grepl()} returns \code{FALSE} for \code{NA} input (its own documented
exception, unlike every other function compared here, which returns
\code{NA}); and \code{RecordLinkage::jarowinkler()} applies its prefix bonus at a
lower similarity threshold, so it agrees with \code{\link[=jaro_winkler]{jaro_winkler()}} exactly on
near-duplicate pairs but diverges on largely-unrelated ones. Anything
else that disagrees is flagged clearly — that's a real finding, worth
investigating rather than ignoring.

In an interactive session it asks for the input size, function family,
and repetition count; any argument supplied explicitly (or every
argument, when called non-interactively) skips its prompt.

Timings come from \code{\link[=system.time]{system.time()}} on your machine, for the input you
choose — they will differ from any number printed elsewhere. For a
reproducible, full-scale (3M-row) measurement across every function, see
\verb{Rscript bench.R --full} or the benchmarks chapter at
\url{https://kylehaynes.github.io/fast.string/06-benchmarks.html}.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
# Prompts for input size, function family, and repetitions:
run_benchmark_example()

# Or skip every prompt by supplying all three:
run_benchmark_example(n = 200000, family = "matching", reps = 3)
\dontshow{\}) # examplesIf}
}
\concept{benchmarking functions}
