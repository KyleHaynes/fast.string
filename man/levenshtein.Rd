% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/levenshtein.R
\name{levenshtein}
\alias{levenshtein}
\alias{damerau_levenshtein}
\alias{hamming}
\title{Vectorised Levenshtein, Damerau-Levenshtein, and Hamming distance}
\usage{
levenshtein(a, b, nthreads = NULL)

damerau_levenshtein(a, b, nthreads = NULL)

hamming(a, b, nthreads = NULL)
}
\arguments{
\item{a, b}{Equal-length character vectors.}

\item{nthreads}{Positive integer per-call thread cap, or \code{NULL} to use the
RcppParallel default. \code{1} forces serial execution.}
}
\value{
Numeric vector the same length as \code{a}, \code{NA} if either \code{a[i]} or
\code{b[i]} is \code{NA}.
}
\description{
Classic edit-distance metrics — \code{stringdist}'s bread and butter — backed
by a bit-parallel implementation rather than \code{stringdist}'s generic
R-level dispatch:
}
\details{
\itemize{
\item \code{levenshtein()} — minimum number of single-character insertions,
deletions, and substitutions to turn \code{a[i]} into \code{b[i]}. Uses Myers'
(1999) bit-vector algorithm — the same family of trick as this
package's bit-parallel Jaro (see \code{jaro_winkler_core.h}) — turning the
usual O(l1\emph{l2) dynamic-programming table into O(l1) word operations
whenever the shorter string is <= 64 bytes (the overwhelming majority
of names/addresses); longer pairs fall back to an O(l1}l2)
rolling-row DP.
\item \code{damerau_levenshtein()} — like Levenshtein, but an adjacent-character
transposition (\code{"ab"} <-> \code{"ba"}) also costs one edit, under the
"optimal string alignment" (OSA) restriction that no substring is
edited more than once. This matches \code{stringdist::stringdist(method = "dl")}, not full unrestricted Damerau-Levenshtein.
\item \code{hamming()} — number of positions at which two equal-length strings
differ. Returns \code{Inf} for a pair of unequal length, matching
\code{stringdist::stringdist(method = "hamming")}.
}

All three run a single parallelised C++ pass over the whole vector (via
Intel TBB through RcppParallel), the same as \code{\link[=jaro_winkler]{jaro_winkler()}}.
}
\examples{
levenshtein("kitten", "sitting")
damerau_levenshtein("ab", "ba")
hamming("karolin", "kathrin")
}
\seealso{
\code{\link[=levenshtein_matrix]{levenshtein_matrix()}}, \code{\link[=damerau_levenshtein_matrix]{damerau_levenshtein_matrix()}}, \code{\link[=jaro_winkler]{jaro_winkler()}}
}
