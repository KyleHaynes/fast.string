% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fuzzy_lookup.R
\name{fuzzy_lookup}
\alias{fuzzy_lookup}
\alias{fuzzy_match}
\alias{fuzzy_top_n}
\title{Memory-efficient fuzzy lookup}
\usage{
fuzzy_match(
  x,
  table,
  method = c("jaro_winkler", "levenshtein", "osa", "damerau_levenshtein"),
  p = 0.1,
  min_score = 0,
  max_distance = NULL,
  match_na = FALSE,
  nomatch = NA_integer_,
  nthreads = NULL,
  use_bytes = FALSE
)

fuzzy_top_n(
  x,
  table,
  method = c("jaro_winkler", "levenshtein", "osa", "damerau_levenshtein"),
  top_n = 5L,
  p = 0.1,
  min_score = 0,
  max_distance = NULL,
  match_na = FALSE,
  nthreads = NULL,
  use_bytes = FALSE
)
}
\arguments{
\item{x}{Character vector of query strings.}

\item{table}{Character vector of candidate strings.}

\item{method}{One of \code{"jaro_winkler"}, \code{"levenshtein"}, \code{"osa"}, or
\code{"damerau_levenshtein"}.}

\item{p}{Jaro-Winkler prefix scale in \verb{[0, 0.25]}.}

\item{min_score}{Minimum normalized similarity in \verb{[0, 1]}.}

\item{max_distance}{Optional non-negative integer raw-distance cutoff for
edit-distance methods. Not available for Jaro-Winkler.}

\item{match_na}{Logical. Match an \code{NA} query to the first \code{NA} in \code{table}.}

\item{nomatch}{Integer scalar returned by \code{\link[=fuzzy_match]{fuzzy_match()}} when no candidate
passes the cutoffs.}

\item{nthreads}{Positive integer per-call thread cap, or \code{NULL} for the
RcppParallel default.}

\item{use_bytes}{Logical. Compare bytes when \code{TRUE}; compare UTF-8 code
points when \code{FALSE}.}

\item{top_n}{Positive integer number of candidates retained per query.}
}
\value{
\code{fuzzy_match()} returns an integer vector of table positions.
\code{fuzzy_top_n()} returns a data frame with \code{query_index}, \code{table_index},
\code{score}, and \code{rank}.
}
\description{
Find the best or top-N matches for each element of \code{x} in \code{table} without
allocating a full \code{length(x)} by \code{length(table)} score matrix. Lookup is
parallelized over queries and retains only the requested matches.
}
\details{
All methods return normalized similarity scores in \verb{[0, 1]}, where one is
an exact match. New lookup APIs compare UTF-8 code points by default; set
\code{use_bytes = TRUE} for byte-wise comparison. Code-point comparison does not
perform Unicode normalization or grapheme-cluster segmentation.
}
\examples{
fuzzy_match(c("SMITH", "JONES"), c("SMYTH", "JONAS", "JONES"))
fuzzy_top_n("kitten", c("sitting", "mitten", "cat"), top_n = 2)
}
