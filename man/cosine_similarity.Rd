% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qgram_metrics.R
\name{cosine_similarity}
\alias{cosine_similarity}
\title{Q-gram cosine similarity}
\usage{
cosine_similarity(a, b, q = 2, nthreads = NULL)
}
\arguments{
\item{a, b}{Equal-length character vectors.}

\item{q}{Q-gram length (default \code{2}, i.e. bigrams — the common default).
Must be a single integer >= 1.}

\item{nthreads}{Positive integer per-call thread cap, or \code{NULL} to use the
RcppParallel default. \code{1} forces serial execution.}
}
\value{
Numeric vector of similarities in \verb{[0, 1]}, \code{length(a)} long.
Missing comparisons return \code{NA}. If neither string has a q-gram the
similarity is \code{1}; if only one does, it is \code{0}.
}
\description{
Builds frequency profiles of overlapping length-\code{q} substrings and returns
their cosine similarity. Unlike \code{\link[=jaccard_index]{jaccard_index()}}, repeated q-grams retain
their multiplicity. Packed integer q-grams and parallel pairwise dispatch
avoid constructing an R-level count table for each comparison.
}
\examples{
cosine_similarity("night", "nacht")
cosine_similarity("aaaa", "aaab", q = 2)
}
\seealso{
\code{\link[=cosine_matrix]{cosine_matrix()}}, \code{\link[=jaccard_index]{jaccard_index()}}
}
