% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/levenshtein.R
\name{edit_distance}
\alias{edit_distance}
\alias{levenshtein}
\alias{osa_distance}
\alias{damerau_levenshtein}
\alias{hamming}
\alias{levenshtein_similarity}
\alias{osa_similarity}
\alias{damerau_levenshtein_similarity}
\title{Parallel edit distances and normalized similarities}
\usage{
levenshtein(a, b, nthreads = NULL, use_bytes = TRUE)

osa_distance(a, b, nthreads = NULL, use_bytes = TRUE)

damerau_levenshtein(a, b, nthreads = NULL, use_bytes = TRUE)

hamming(a, b, nthreads = NULL, use_bytes = TRUE)

levenshtein_similarity(a, b, nthreads = NULL, use_bytes = TRUE)

osa_similarity(a, b, nthreads = NULL, use_bytes = TRUE)

damerau_levenshtein_similarity(a, b, nthreads = NULL, use_bytes = TRUE)
}
\arguments{
\item{a, b}{Equal-length character vectors.}

\item{nthreads}{Positive integer per-call thread cap, or \code{NULL} for the
RcppParallel default.}

\item{use_bytes}{Logical scalar. Compare encoded bytes when \code{TRUE}, or
UTF-8 code points when \code{FALSE}.}
}
\value{
A numeric vector the same length as \code{a}; \code{NA} when either input is
\code{NA}. Distance functions return raw edit counts. Similarity functions
return values in \verb{[0, 1]}.
}
\description{
\code{levenshtein()} counts insertions, deletions, and substitutions.
\code{osa_distance()} additionally permits adjacent transpositions under the
Optimal String Alignment restriction. \code{damerau_levenshtein()} implements
unrestricted Damerau-Levenshtein, where a substring can participate in
more than one edit. \code{hamming()} counts differing positions and returns
\code{Inf} for unequal-length strings.
}
\details{
Existing functions default to byte comparison for compatibility. Set
\code{use_bytes = FALSE} to compare UTF-8 code points. This is code-point
comparison, not grapheme-cluster comparison or Unicode normalization.
}
\examples{
levenshtein("kitten", "sitting")
osa_distance("ca", "abc")
damerau_levenshtein("ca", "abc")
levenshtein_similarity("kitten", "sitting")
}
